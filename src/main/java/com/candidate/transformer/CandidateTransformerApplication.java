package com.candidate.transformer;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application entry point for the Candidate Profile Transformer CLI.
 */
@SpringBootApplication
public class CandidateTransformerApplication {

    public static void main(String[] args) {
        SpringApplication.run(CandidateTransformerApplication.class, args);
    }
}
